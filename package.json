{
  "name": "fastify-boilerplate-service",
  "version": "1.0.0",
  "description": "Fastify API",
  "main": "index.js",
  "scripts": {
    "coverage": "standard --fix && tap test -bCRlist",
    "test": "standard --fix && tap test -bCRlist --no-coverage"
  },
  "author": "Gonzalo Spina",
  "license": "ISC",
  "dependencies": {
    "fastify": "^2.15.1",
    "flatstr": "^1.0.12",
    "readable-stream": "^3.4.0",
    "under-pressure": "^3.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.3",
    "proxyquire": "^2.1.3",
    "standard": "*",
    "tap": "^14.6.9"
  }
}
